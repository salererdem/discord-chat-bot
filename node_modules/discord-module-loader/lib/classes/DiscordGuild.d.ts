import { Collection } from "discord.js";
import type DiscordCommand from "./DiscordCommand";
import type DiscordEvent from "./DiscordEvent";
import type DiscordModule from "./DiscordModule";
import type { Snowflake } from "discord.js";
export default class DiscordGuild {
    id: Snowflake;
    callback?: any;
    disabled: boolean;
    events: Collection<string, DiscordEvent<any>>;
    commands: Collection<string, DiscordCommand>;
    modules: Collection<string, DiscordModule>;
    constructor(id: Snowflake, callback?: any, options?: {
        disabled: boolean;
    });
    enable(): void;
    disable(): void;
}
